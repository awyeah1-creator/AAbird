# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
# IntelliJ related
*.iml
*.ipr
*.iws
.idea/
# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/
# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
# Symbolication related
app.*.symbols
# Obfuscation related
app.*.map.json
# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
# API Keys - Never commit these!
lib/apikeys.dart
*.env
.env*
assets/
wagtail_promts.dart
lib/integrations/perplexity/wagtail_prompts.dart
aabird-clean/
.gradle/
local.properties
# Android signing keys - Never commit these!
*.keystore
# Flutter plugins - May be regenerated
.flutter-plugins
# Flutter package config (legacy)
.packages
# iOS build output (not needed in version control)
/ios/build/
# Android build output (not needed in version control)
/android/build/
# Crashlytics and Firebase build output
crashlytics/
# Test results and artifacts
test-results/
# macOS build output
/macos/build/
# web build output
